# Supabase — get these from your project: Settings → API
# When both are set (locally in .env.local and in Vercel), the app uses real
# Supabase Auth. When unset, it falls back to local placeholder auth.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
